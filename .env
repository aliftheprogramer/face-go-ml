# === Face Attendance ML defaults ===
# Webhook URL of your main Attendance API. Leave empty to disable dispatch.
ATTENDANCE_WEBHOOK_URL=http://127.0.0.1:8000/mock/webhook

# Optional API key to be sent as Authorization: Bearer <ATTENDANCE_API_KEY>
ATTENDANCE_API_KEY=

# Cooldown to avoid duplicate attendance events (seconds)
EVENT_COOLDOWN_SECONDS=5

# Face match tolerance (smaller = stricter). If unset, default is 0.45 in code.
# FACE_TOLERANCE=0.45

# Enable/disable local attendance recording (for demo/testing)
ATTENDANCE_LOCAL_ENABLED=true

# Local attendance deduplication window (seconds)
# - Set to 60 for demo: a student can only be counted once per minute
# - Set to 0 for strict once-per-day (no increment again within the same date)
ATTENDANCE_LOCAL_DEDUP_SECONDS=60

# Optional: override DB path for students
# STUDENTS_DB_PATH=./data/students.db
